我要做一个基于JDN的历史无尽时间系统，其中的一个核心就是JDN与公历的转换，基于公历之上，再进行诸如农历和年号的转换。
但在重构过程中，程序和测试出现了混乱。请帮我分析代码和我的意图，将程序修正。





接下来是工程问题：
1. 绘制时间轴时，我应该怎样决定绘制刻度的时间点（包括主刻度和副刻度）？比如，刻度为月，日，时甚至年。
2. 如何设计缩放的step和机制，理想的缩放情况应该是无缝衔接（包括主刻度和副刻度）。即随着放大，在保持大刻度不跳变的情况下下一级小的单位刻度逐步呈现。同理在缩小的过程中，下一级的刻度缩小后大的刻度也无缝出现，同样没有跳变。




有几个问题：
1. 层级从小开始，因为大无上限。
2. 如何“计算”、“发现”或“迭代”（下一个）刻度。
3. 刻度层级应该更平滑，毕竟用户可能更关注从日到百年之内的视图。
4. 在超过一定范围之后，就不应该计算和显示更详细的信息了（比如史前一亿年，只有年有意义了）。
如何处理这些问题？





请结合上下文，正式实现一个TickStepper，并包含精心设计的，合理的step列表。




实现一个基于pyqt5的时间坐标系demo，直接使用qpaint在窗口中心绘制时间轴。支持以下操作：
1. 鼠标拖动时间轴
2. 滚轮滚动时间轴
3. 按ctrl + 滚轮缩放时间轴
4. 支持水平绘制和竖直绘制，提出一套统一处理和绘制的方法，使用这种方法只需要一套代码，而不需要通过if else判断是水平还是竖直写两套代码。



















